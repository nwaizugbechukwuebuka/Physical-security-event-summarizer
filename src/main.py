import logging
from src.api.routes import api_router

def main():
    logging.basicConfig(level=logging.INFO)
    print("Physical Security Event Summarizer is running.")
    # Placeholder for actual app startup (e.g., FastAPI, CLI, etc.)

if __name__ == "__main__":
    main()
